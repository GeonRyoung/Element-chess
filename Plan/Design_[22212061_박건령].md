## 1. Introduction
#### Summary
본 문서는 'Net-Iocp' 프로젝트의 분석 단계를 거쳐 도출된 요구된 요구사항을 바탕으로, 시스탬의 구체적인 설계 방안을 기술한 최종 기술 설계서이다. 본설계서는 시스템의 정적 구조를 정의하는 클래스 다이어그램, 객체간의 동적 상호작용을 나타내는 시퀀스 다이어그램, 그리고 시스템의 상태 변화를 명시하는 상태 머신 다이어그램을 상세히 정의한다. 이를 통해 실제 구현 단계에서 발생할 수 있는 모호성을 제기하고, 개발자에게 명확한 구현 가이드를 제공하는 것을 목적으로 한다.

#### Introduce
본 시스템은 대규모 동시 접속자가 발생하는 환경에서도 낮은 지연 시간과 높은 처리량을 보장해야 하는 고성능 게임 서버 엔진을 지향한다. 시스템의 핵심 기술적 특징은 다음과 같다.
- **IOCP 기반 비동기 엔진**: Windows 플랫폼의 I/O 모델인 IOCP를 네트워크 최하단 계층에 배치하여 비동기 처리 성능을 극대화한다.
- **RUDP**: UDP의 빠른 전송 속도를 유지하면서도 애플리케이션 계층이ㅔ서 Reliable UDP 매커니즘을 구현하여 데이터 전송의 신뢰성을 확보한다.
- **공간 분할 최적화**: 수많ㅇ느 객체 간의 상호작용 연산 비용을 절감하기 위해 Sector 기반 공간 분할 알고리즘을 적용한다.
- **Modern C++ 표준 활용**: C++/17/20 표준의 기능을 적극 도입하여 메모리 안전성을 높이고 효율적인 동시성 제어가 가능한 아키텍처를 설계한다.

#### Goals
본 설계의 최종 목표는 고성능 및 고신뢰성을 갖춘 서버 아케틱철르 구축하는 것이며, 세부 목표는 다음과 같다.
- **구현 모호성 제거**: 분석 단계의 유스케이스를 구체적인 시퀀스 및 클래스 구조로 변환하여 설계와 구현 간의 간극을 최소화한다.
- **시스템 확장성 확보**: 비동기 엔진과 공간 분할 알고리즘을 통해 대규모 접속자 수용이 가능한 확장성 있는 구조를 설계한다.
- **안정적인 상태 관리**: 클라이언트와 서버의 복잡한 상태 변화를 상태 머신 다이어그램으로 정의하여 예외상황에서도 안정적인 서비스 유지를 가능하게 한다.
- **효율적인 자원 관리**: 최신 C++ 표준 기술을 활용하여 불필요한 자원 낭비를 줄이고 서버 운여의 효율성을 극대화한다.

## 2. Class Diagram
본 시스템은 크게 네트워크 입출력을 담당하는 ```IOCP Layer```, 세션 및 패킷 신뢰성을 관리하는 ```Session Layer```, 게임의 비즈니스 로직을 처리하는 ```Logic Layer```, 그리고 시스템의 상태를 감시하는 ```Monitoring Layer```로 계층화하여 설계되었다. 각 계층은 인터페이스를 통해 결합도를 낮추고 독립적인 테스트와 확장을 용이하게 한다.

### Core System Classes Overview

### Class Specifications

본 절에서는 분석 단계에서 도출된 18종의 핵심 도메인 모델을 바탕으로, 실제 구현에 필요한 속성과 메서드를 구체화한다. 모든 설계는 C++17/20 표준의 현대적 타입 시스템과 IOCP의 비동기 메커니즘을 최우선으로 고려하였다.

---

#### 1) IocpCore
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| hIocp | HANDLE | Private | Windows 커널 객체인 IOCP 핸들을 관리하며 시스템 레벨 리소스를 캡슐화함. |
| workerThreads | std::vector\<std::thread\> | Private | CPU 코어 수에 최적화된 워커 스레드들을 관리하여 병렬 처리 성능을 극대화함. |
| isRunning | std::atomic\<bool\> | Private | 서버 가동 상태 플래그로, 스레드 간 안전한 종료 시그널링을 수행함. |
| sessionManager | std::shared_ptr\<SessionManager\> | Private | 세션의 생명주기를 전담 관리하는 매니저 객체에 대한 소유권을 가짐. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| createIocpPort | bool | Public | (DWORD) | IOCP 포트를 생성하고 초기화하여 비동기 통신의 진입점을 제공함. |
| registerSocket | bool | Public | (SOCKET, SessionPtr) | 소켓과 세션 객체를 IOCP에 바인딩하여 커널 레벨의 완료 통지를 활성화함. |
| workerThreadMain | void | Private | () | GQCS를 호출하여 비동기 작업 완료를 대기하고 이벤트를 처리하는 핵심 루프임. |

---

#### 2) SessionManager
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| sessions | std::unordered_map\<...\> | Private | 세션 ID를 키로 하여 활성화된 세션들을 관리하고 빠른 조회를 지원함. |
| sessionPool | std::shared_ptr\<ObjectPool\> | Private | 세션 객체 생성/소멸 시의 메모리 단편화와 할당 오버헤드를 방지함. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| createSession | SessionPtr | Public | (SOCKET) | 오브젝트 풀에서 세션을 할당받아 초기화하고 관리 목록에 추가함. |
| releaseSession | void | Public | (uint32_t) | 종료된 세션을 목록에서 제거하고 리소스를 안전하게 풀로 반환함. |

---

#### 3) RudpSession
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| sessionId | uint32_t | Private | 전역에서 유일한 세션 식별자로 로그 추적 및 세션 구분에 사용됨. |
| hSocket | SOCKET | Private | 실제 통신에 사용되는 UDP 소켓 핸들을 관리함. |
| recvBuffer | CircularBuffer* | Private | 수신된 파편화 데이터를 임시 저장하고 순차적인 패킷 조립을 수행함. |
| sequenceNumber | std::atomic\<uint32_t\> | Private | RUDP 신뢰성 확보를 위한 송신 시퀀스 번호를 원자적으로 관리함. |
| remoteAddr | SOCKADDR_IN | Private | 비연결형 UDP에서 상대방의 주소 정보를 식별하기 위해 저장함. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| handleReceive | void | Public | (DWORD, char*) | 비동기 수신 완료 시 데이터 유효성을 검사하고 패킷 핸들러로 전달함. |
| sendPacket | void | Public | (PacketPtr) | 패킷에 시퀀스를 부여하고 Sliding Window 알고리즘에 따라 전송함. |
| processAck | void | Public | (uint32_t) | 수신된 ACK를 분석하여 재전송 큐에서 확인된 패킷을 제거함. |

---

#### 4) CircularBuffer
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| buffer | std::vector\<char\> | Private | 실제 바이트 데이터가 저장되는 연속된 메모리 공간임. |
| readPos | size_t | Private | 버퍼에서 다음에 읽을 데이터의 위치 인덱스임. |
| writePos | size_t | Private | 버퍼에서 다음에 기록할 데이터의 위치 인덱스임. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| pushData | bool | Public | (const char*, int) | 외부 데이터를 버퍼에 기록하며 가용 공간 여부를 확인함. |
| popData | bool | Public | (int) | 지정된 크기만큼 데이터를 소비한 것으로 간주하여 읽기 위치를 갱신함. |
| peekData | const char* | Public | () | 데이터를 제거하지 않고 현재 위치의 데이터 포인터를 반환함. |

---

#### 5) PacketHandler
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| handlerMap | std::unordered_map\<...\> | Private | 패킷 Opcode와 처리 함수를 매핑하여 전략 패턴 기반의 확장을 지원함. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| registerHandler | void | Public | (uint16_t, HandlerFunc) | 새로운 패킷 타입에 대한 처리 로직을 런타임에 등록함. |
| dispatch | void | Public | (SessionPtr, PacketPtr) | 패킷의 헤더를 분석하여 적절한 처리 작업(Job)을 생성하고 분기함. |

---

#### 6) JobQueue
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| jobQueue | std::queue\<JobFunc\> | Private | 네트워크 스레드와 로직 스레드 간의 비동기 작업을 전달하는 큐임. |
| queueLock | std::mutex | Private | 멀티스레드 환경에서 큐 접근 시 데이터 무결성을 보장하기 위한 락임. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| pushJob | void | Public | (JobFunc) | 네트워크 이벤트로 발생한 로직 작업을 큐에 안전하게 삽입함. |
| popAllJobs | void | Public | (std::vector\<JobFunc\>&) | 로직 스레드에서 큐의 모든 작업을 한 번에 추출하여 처리 효율을 높임. |

---

#### 7) IExecutionService (Interface)
**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| execute | void | Public | (JobFunc) | 특정 실행 문맥(Context)에서 작업을 수행하도록 정의하는 순수 가상 함수임. |

---

#### 8) GameLogic
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| world | std::shared_ptr\<World\> | Private | 게임 월드의 모든 엔티티와 상태를 관리하는 객체를 소유함. |
| jobQueue | std::shared_ptr\<JobQueue\> | Private | 네트워크 계층에서 전달된 비즈니스 로직 작업들을 수신하는 통로임. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| update | void | Public | (float) | 매 프레임 호출되어 누적된 작업을 처리하고 월드 시뮬레이션을 갱신함. |
| processJobs | void | Private | () | JobQueue에서 작업을 꺼내어 실제 게임 규칙(이동, 전투 등)을 실행함. |

---

#### 9) World
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| sectors | std::vector\<...\> | Private | 전체 공간을 격자(Sector)로 나누어 연산 범위를 제한하고 최적화함. |
| entityMap | std::unordered_map\<...\> | Private | 월드 내 모든 객체를 전역 ID로 빠르게 조회하기 위한 전역 관리 맵임. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| getSector | SectorPtr | Public | (float, float) | 월드 좌표를 기반으로 해당 위치가 속한 섹터 객체를 $O(1)$로 반환함. |
| updateWorldTick | void | Public | () | 월드의 논리적 시간을 갱신하고 시간에 의존하는 이벤트를 처리함. |

---

#### 10) Sector
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| sectorId | uint32_t | Private | 공간 내 섹터의 고유 번호로 이웃 섹터 계산 등에 활용됨. |
| entities | std::unordered_set\<...\> | Private | 현재 섹터 영역에 머무르고 있는 엔티티들의 목록을 관리함. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| addEntity | void | Public | (EntityPtr) | 새로운 객체가 섹터로 진입 시 목록에 추가하고 주변에 알림을 전송함. |
| broadcastinRadius | void | Public | (PacketPtr) | 해당 섹터 및 인접 섹터의 객체들에게만 패킷을 전송하여 트래픽을 최적화함. |

---

#### 11) Entity (Abstract)
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| entityId | uint32_t | Protected | 모든 월드 객체가 가지는 고유 식별 번호임. |
| position | Vector2 | Protected | 객체의 월드 내 현재 2D 위치 좌표임. |
| currentSector | SectorPtr | Protected | 객체가 속한 섹터 정보를 저장하여 섹터 변경 감지 및 브로드캐스트에 활용함. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| update | void | Public | (float) | 하위 클래스에서 구현할 로직 갱신 추상 메서드임. |
| setPosition | void | Public | (Vector2) | 위치를 갱신하고 소속 섹터가 바뀌었는지 판단하여 갱신 로직을 수행함. |

---

#### 12) Player
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| session | std::weak_ptr\<RudpSession\> | Private | 네트워크 세션과 연결되어 실제 클라이언트와의 통신 통로 역할을 함. |
| username | std::string | Private | 사용자 식별을 위한 고유 닉네임 또는 계정 이름임. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| sendPacket | void | Public | (PacketPtr) | 플레이어 소유 세션을 통해 특정 데이터를 클라이언트에게 송신함. |
| update | void | Public | (float) | 플레이어 고유의 상태 변화 및 비즈니스 로직을 수행함. |

---

#### 13) Bot
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| fsmState | BotState | Private | 봇의 현재 AI 상태(배회, 추적, 대기 등)를 관리함. |
| targetPos | Vector2 | Private | 봇이 이동하고자 하는 목표 지점의 좌표임. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| processAI | void | Public | () | 정해진 규칙에 따라 행동을 결정하고 상태 전이를 수행함. |
| update | void | Public | (float) | 봇의 행동 주기(AI Tick)와 물리적 이동을 갱신함. |

---

#### 14) BotManager
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| dummyClients | std::vector\<BotPtr\> | Private | 부하 테스트를 위해 생성된 가상 클라이언트 객체들을 관리함. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| spawnBots | void | Public | (int) | 지정된 수만큼 봇을 일괄 생성하여 월드에 진입시킴. |
| startSimulation | void | Public | () | 각 봇의 AI를 활성화하여 서버에 지속적인 트래픽 부하를 발생시킴. |

---

#### 15) TimerWheel
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| wheel | std::vector\<...\> | Private | 틱 단위로 예약된 작업들을 환형 큐 구조로 분산 저장하여 관리함. |
| currentTick | std::atomic\<uint64_t\> | Private | 시스템 가동 후 경과된 전체 틱 수를 추적함. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| addTimer | void | Public | (uint32_t, Callback) | 일정 시간 후에 실행될 콜백 작업을 $O(1)$의 비용으로 예약함. |
| tick | void | Public | () | 매 주기에 호출되어 현재 틱에 해당하는 만료된 타이머들을 실행함. |

---

#### 16) MetricManager
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| packetCounter | Counter | Private | 실시간으로 처리되는 패킷의 총량을 누적하여 Throughput을 측정함. |
| latencyGauge | Gauge | Private | 네트워크 응답 시간이나 로직 지연 시간을 실시간 수치로 관리함. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| recordPacket | void | Public | () | 패킷 수신 또는 송신 이벤트 시 메트릭 값을 갱신함. |
| exposeMetrics | std::string | Public | () | 수집된 데이터를 Prometheus 등의 외부 시스템이 읽을 수 있게 직렬화함. |

---

#### 17) ObjectPool
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| freeList | std::stack\<...\> | Private | 즉시 재사용 가능한 유휴 객체들의 포인터를 관리함. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| alloc | std::shared_ptr\<T\> | Public | () | 풀에서 객체를 즉시 반환하며, 필요 시 새로운 청크를 할당함. |
| free | void | Public | (std::shared_ptr\<T\>) | 사용이 끝난 객체를 초기화한 후 다시 유휴 목록에 추가함. |

---

#### 18) Packet
**Attributes**
| Name | Type | Visibility | Description & Design Intent |
| :--- | :--- | :--- | :--- |
| header | PacketHeader | Private | Opcode, 크기, 시퀀스 번호 등 패킷의 공통 제어 정보를 저장함. |
| payload | std::vector\<char\> | Private | 직렬화되어 바이트 배열로 변환된 실제 게임 데이터임. |

**Methods**
| Name | Return Type | Visibility | Parameters | Description & Design Intent |
| :--- | :--- | :--- | :--- | :--- |
| serialize | void | Public | (T) | 구조체나 클래스 데이터를 네트워크 전송이 가능한 바이트 스트림으로 변환함. |
| deserialize | T | Public | () | 수신된 바이트 스트림을 원래의 비즈니스 객체 타입으로 복원함. |

---

## 3. Sequence Diagram

#### 1) 대규모 세션 접속 및 인증
1. **Connection Request**: `BotManager::SpawnBots()`가 호출되어 Dummy Client를 생성하고 `IocpCore`로 연결 요청을 보낸다.
2. **Accept & Alloc**: `IocpCore`는 요청을 수락하고 `ObjectPool::Alloc<RudpSession>()`을 호출하여 세션 객체를 할당한다.
3. **Register Handle**: `IocpCore::RegisterSocket()`을 통해 클라이언트 소켓을 IOCP에 등록한다.
4. **Auth Request**: Dummy Client가 `PKT_CS_AUTH_REQ` 패킷을 전송한다.
5. **Receive Event**: `IocpCore::WorkerThreadMain()` 루프에서 GQCS 이벤트를 감지하고 `RudpSession::HandleReceive()`를 호출한다.
6. **Dispatch**: `RudpSession`은 패킷 조립 후 `PacketHandler::Dispatch()`를 호출한다.
7. **Authentication Logic**: `PacketHandler`는 등록된 인증 핸들러를 실행하고, 성공 시 플레이어를 월드에 등록한다.
8. **Auth Response**: 서버가 `PKT_SC_AUTH_SUCCESS`를 클라이언트에게 전송한다.

#### 2) 실시간 위치 업데이트
1. **Move Packet Send**: Dummy Client가 위치 정보가 담긴 패킷(Unreliable)을 전송한다.
2. **IOCP Dispatch**: `IocpCore::WorkerThreadMain()`를 거쳐 `RudpSession::HandleReceive()`가 호출된다.
3. **Routing**: `PacketHandler::Dispatch()`는 패킷 타입을 확인하고 이동 로직 핸들러로 라우팅한다.
4. **Update Position**: 핸들러는 `World::GetSector(x, y)`를 호출하여 현재 섹터를 확인하고 위치를 갱신한다.
5. **Sector Check**: 섹터 변경이 발생하면 이전 섹터에서 제거하고 `NewSector::AddEntity()`를 호출한다.
6. **Broadcast**: `Sector::BroadcastInRadius()`를 호출하여 인접한 섹터의 클라이언트들에게 위치 갱신 패킷을 전송한다.

#### 3) 전투 이벤트 동기화
1. **Attack Packet Send**: Dummy Client가 공격 패킷(Reliable)을 전송하고 `TimerWheel::AddTimer()`를 통해 재전송 타이머를 시작한다.
2. **Server Processing**: 서버는 패킷을 수신하고 `PacketHandler::Dispatch()`를 통해 데미지 로직을 수행한다.
3. **Send ACK**: 서버는 수신 확인(ACK) 패킷을 송신한다.
4. **Receive ACK**: 클라이언트가 ACK를 수신하면 `RudpSession::ProcessAck()`를 호출하여 타이머를 해제한다.
5. **Timeout & Retransmit**: 지정된 시간 내에 ACK가 도착하지 않으면 `TimerWheel::Tick()`이 타임아웃 이벤트를 발생시키고 패킷을 재전송한다.

#### 4) 네트워크 장애 시뮬레이션 설정
1. **Config Request**: 관리자나 테스트 스크립트가 `NetworkLayer`에 장애 설정을 요청한다.
2. **Set Parameters**: 패킷 손실률(Loss Rate)과 지연 시간(Latency)을 설정한다.
3. **Packet Drop Simulation**: 이후 송수신되는 패킷들은 설정된 확률에 따라 폐기되거나 지연 큐에 진입한다.
4. **Monitoring**: `MetricManager`는 손실된 패킷 수를 카운팅하여 모니터링 데이터에 반영한다.

#### 5) 섹터 기반 동기화 제어
1. **Calculate Index**: 객체 좌표 $(x, y)$를 기반으로 섹터 인덱스 $Index = (y / SectorHeight) \times Width + (x / SectorWidth)$를 계산한다.
2. **Retrieve Sector**: 계산된 인덱스를 통해 해당 섹터 객체를 반환한다.
3. **Interest Management**: 섹터 및 인접 8개 섹터의 엔티티만 순회하여 연산 범위를 제한한다.
4. **Filtering**: 시야 거리(Vision Radius)를 벗어난 엔티티는 패킷 전송 대상에서 제외하여 트래픽을 최적화한다.

#### 6) 실시간 서버 메트릭 노출
1. **Event Occurrence**: 패킷 수신, 세션 접속 등의 이벤트가 발생한다.
2. **Update Metric**: 각 핸들러는 `MetricManager`를 호출하여 메트릭을 실시간으로 갱신한다.
3. **Scraping Request**: 외부 Prometheus 서버가 주기적으로 HTTP 요청을 보낸다.
4. **Expose Data**: `MetricManager::ExposeMetrics()`가 수집된 데이터를 Prometheus 표준 포맷으로 응답한다.

#### 7) 통합 대시보드 시각화
1. **Data Push**: Prometheus가 수집한 시계열 데이터를 저장한다.
2. **Query Request**: 관리자가 웹 브라우저를 통해 Grafana 대시보드에 접속한다.
3. **Visualization**: Grafana는 Prometheus에 쿼리를 보내 PPS, CPU 사용량 등의 데이터를 가져와 그래프로 렌더링한다.

#### 8) 지표 임계치 기반 알람
1. **Check Threshold**: 시스템은 주기적으로 현재 부하가 설정된 임계치를 초과했는지 확인한다.
2. **Trigger Alert**: 임계치 초과 시 알람 이벤트를 발생시킨다.
3. **Notification**: 관리자에게 이메일이나 메신저를 통해 경고 메시지를 전달한다.

#### 9) 동적 봇 행동 패턴 제어
1. **Update Loop**: `BotManager::StartSimulation()`이 매 프레임 호출된다.
2. **State Transition**: 각 Bot 객체는 자신의 FSM 상태를 확인하고 전이 조건(예: Idle → Move)을 체크한다.
3. **Action Execution**: 상태에 따라 적절한 패킷을 생성하여 전송한다.
4. **Random Pattern**: 일정 확률로 행동 패턴을 변경하여 실제 유저와 유사한 트래픽을 생성한다.

#### 10) 세션 종료 및 자원 회수
1. **Disconnect Request**: 클라이언트가 접속 종료 패킷을 보내거나 소켓을 닫는다.
2. **Event Detection**: `IocpCore` 워커 스레드에서 연결 해제 이벤트를 감지한다.
3. **Cleanup Logic**: `RudpSession`의 종료 처리를 수행하고 월드에서 해당 엔티티를 제거한다.
4. **Resource Return**: `ObjectPool::Free(session)`을 호출하여 세션 객체를 메모리 풀로 반환한다.

---

## 4. State Machine Diagram

시스템 내 핵심 객체들의 라이프사이클과 상태 전이 조건을 정의한다. 특히 RUDP의 신뢰성 확보를 위한 `TimerWheel`과의 연계 동작과 `sequenceNumber` 관리를 포함하여 설계한다.

### 4.1. Client (Bot) State Machine

클라이언트(Bot)는 서버와의 연결 상태 및 인증 단계에 따라 다음과 같은 상태 변화를 거친다.

| State Name | Description & Transition Trigger |
| :--- | :--- |
| **연결 해제** | 초기 상태 또는 네트워크 연결이 해제된 상태이다. <br> - **Transition**: `BotManager::SpawnBots()` 호출 시 '연결 중' 상태로 전이한다. |
| **연결 중** | 서버와의 핸드셰이크 및 소켓 연결을 시도 중인 상태이다. <br> - **Transition**: `connect()` 성공 및 `PKT_CS_AUTH_REQ` 송신 시 '인증 완료' 상태로 전이한다. |
| **인증 완료** | 서버로부터 인증 응답을 수신하고 로그인이 완료된 상태이다. <br> - **Transition**: 초기 월드 데이터 로딩 완료 시 '동기화 중' 상태로 전이한다. |
| **동기화 중** | 정상적으로 게임 플레이를 수행하며 이동 및 전투 패킷을 송수신하는 상태이다. <br> - **Transition**: 로그아웃 요청 또는 비정상 종료 발생 시 '종료' 상태로 전이한다. |
| **종료** | 세션이 종료된 상태이다. <br> - **Transition**: 모든 자원 정리 후 다시 '연결 해제' 상태로 복귀한다. |

---

### 4.2. Server Session State Machine

`RudpSession` 객체는 메모리 효율을 위해 `ObjectPool`에 의해 관리되며, 수신된 패킷의 유효성과 네트워크 이벤트에 따라 상태가 전이된다.

| State Name | Description & Transition Trigger |
| :--- | :--- |
| **대기 / 풀** | `ObjectPool` 내에서 재사용을 위해 대기 중인 상태이다. <br> - **Transition**: `accept()` 이벤트 발생 시 '수락' 상태로 전이한다. |
| **수락** | UDP 소켓이 할당되었으나 아직 IOCP 커널 객체에 등록되지 않은 상태이다. <br> - **Transition**: `IocpCore::RegisterSocket()` 완료 시 '연결 성립' 상태로 전이한다. |
| **연결 성립** | 네트워크 계층의 연결이 완료되었으며, 클라이언트의 논리적 인증 패킷을 대기하는 상태이다. <br> - **Transition**: `PacketHandler`에서 인증 로직을 통과할 경우 '활성' 상태로 전이한다. |
| **활성** | 실시간 데이터 송수신이 활성화된 상태이다. `sequenceNumber`를 관리하며, Reliable 패킷 전송 시 `TimerWheel::AddTimer()`를 통해 ACK 수신을 대기한다. <br> - **Transition**: `OnDisconnected()` 콜백 호출 또는 하트비트 타임아웃 시 '닫힘' 상태로 전이한다. |
| **닫힘** | 소켓 리소스를 닫고 세션의 사후 정리를 진행 중인 상태이다. <br> - **Transition**: `ObjectPool::Free()` 호출을 통해 '대기 / 풀' 상태로 객체를 반환한다. |

